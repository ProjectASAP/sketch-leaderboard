dist
node_modules

# Generated by aqpbm-core's `schema_export_is_current` test and copied in
# verbatim, so it is vendored output rather than source. Left unformatted on
# purpose: kept byte-identical to the Rust export, a stale copy can be found by
# diffing the two files, which reformatting here would defeat.
src/utils/schema.json
